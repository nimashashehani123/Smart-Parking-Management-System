spring:
  application:
    name: api-gateway
  cloud:
    loadbalancer:
      rule: random
    gateway:
      routes:

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/
