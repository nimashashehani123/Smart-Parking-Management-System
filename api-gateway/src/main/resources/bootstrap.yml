spring:
  application:
    name: api-gateway
  cloud:
    config:
      uri: http://localhost:8888 # Make sure this is correct



eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka

